# =============================================================================
# Este archivo es un ESPEJO del canónico en la raíz del repo.
# Preferir siempre:
#   pip install -r ../requirements.txt
# (desde src/) o:
#   pip install -r requirements.txt
# desde la raíz del proyecto.
# Actualizado: 2026-07-14 — ver ../requirements.txt para notas CTRL_ENV / torch.
# =============================================================================

-r ../requirements.txt
